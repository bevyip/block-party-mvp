# Gemini API key for sprite analysis and view generation (optional).
# Get a key at https://aistudio.google.com/app/apikey
# If not set, conversion falls back to peg detection only (no AI views).
VITE_GEMINI_API_KEY=your_api_key_here
