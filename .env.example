# Gemini API key for sprite analysis and view generation (server-side only).
# Set in Vercel: Project → Settings → Environment Variables.
# Get a key at https://aistudio.google.com/app/apikey
# If not set, /api/gemini returns 500 and conversion falls back to canvas pipeline.
GEMINI_API_KEY=your_api_key_here
